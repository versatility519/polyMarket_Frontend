// import { Outlet } from "react-router";
import MainPage from "../pages/market";

const MainLayout = () => {
    return (
        <div>
            <MainPage />
        </div>
    )
}

export default MainLayout;