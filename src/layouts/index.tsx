// import { Outlet } from "react-router";
import MainPage from "../pages/main";

const MainLayout = () => {
    return (
        <div>
            <MainPage />
        </div>
    )
}

export default MainLayout;